name: CoderCo custom action CI

on: [push]

jobs:
  build:
   runs-on: ubuntu-latest

   steps:
   - name: checkout
     uses: actions/checkout@v2

   - name: using custom action 
     uses: iKarmaaa123/coderco-custom@main
     with:
       who-to-greet: 'CoderCo Team'
